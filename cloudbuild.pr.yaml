logsBucket: gs://aridinsights-cb-logs-us-central1
options:
  logging: GCS_ONLY

steps:
  - name: node:18-alpine
    entrypoint: sh
    args:
      - -c
      - |
        npm ci
        npm run lint
        npm run build
options:
  logging: CLOUD_LOGGING_ONLY
timeout: 1200s
